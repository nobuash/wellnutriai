import { privateMetadata } from '@/lib/seo';
import InstallAppPage from './page-client';

export const metadata = privateMetadata('Instalar no celular — WellNutriAI');

export default InstallAppPage;
