import { privateMetadata } from '@/lib/seo';
import PhotoAnalysisPage from './page-client';

export const metadata = privateMetadata('Análise de refeição por foto — WellNutriAI');

export default PhotoAnalysisPage;
