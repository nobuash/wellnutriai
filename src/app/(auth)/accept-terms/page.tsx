import { privateMetadata } from '@/lib/seo';
import AcceptTermsPage from './page-client';

export const metadata = privateMetadata('Termo de Responsabilidade — WellNutriAI');

export default AcceptTermsPage;
