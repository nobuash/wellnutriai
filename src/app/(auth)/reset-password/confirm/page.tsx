import { privateMetadata } from '@/lib/seo';
import ConfirmResetPasswordPage from './page-client';

export const metadata = privateMetadata('Confirmar redefinição de senha — WellNutriAI');

export default ConfirmResetPasswordPage;
