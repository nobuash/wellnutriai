import { privateMetadata } from '@/lib/seo';
import ResetPasswordPage from './page-client';

export const metadata = privateMetadata('Redefinir senha — WellNutriAI');

export default ResetPasswordPage;
