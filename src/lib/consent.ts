// Versões dos termos/privacidade. Suba estes valores (ex: incrementar
// o número) sempre que o texto legal mudar de forma relevante — isso
// força um novo aceite explícito na próxima visita do usuário.
export const TERMS_VERSION = process.env.TERMS_VERSION || '1';
export const PRIVACY_VERSION = process.env.PRIVACY_VERSION || '1';
